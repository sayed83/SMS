
@model MvcUMS.Models.TimeShedule
@{
    ViewBag.Title = "TimeShedule";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row">
    <div class="col-md-offset-3 col-md-6">

        <!-- Basic layout-->
        @using (Html.BeginForm("TimeShedule", "Event", FormMethod.Post, new
        {

            @class = "form-horizontal",
            id = "submitForm"
        }))
        {

            <div class="panel panel-white">
                <div class="panel-heading">
                    <h2 class="panel-title">Time Scheduling</h2>
                </div>

                <div class="panel-body">


                    @*<button type="button" class="close" data-dismiss="alert"><span>&times;</span><span class="sr-only">Close</span></button>*@
                    <span class="error-info text-semibold">@TempData["error"]</span>




                    <div class="form-group">
                        <label class="col-lg-4 control-label">Start time:</label>
                        <div class="col-lg-8">
                            @Html.TextBoxFor(model => model.StartTime, new { @class = "form-control", id = "timetext", placeholder = "Time" })
                            @Html.ValidationMessageFor(model => model.StartTime)
                        </div>
                    </div>


                    <div class="form-group">
                        <label class="col-lg-4 control-label">End Time:</label>
                        <div class="col-lg-8">
                            @Html.TextBoxFor(model => model.EndTime, new { @class = "form-control", id = "endtime", placeholder = "Time" })
                            @Html.ValidationMessageFor(model => model.EndTime)
                        </div>
                    </div>

                    <div class="text-right">
                        <span class="pull-left">
                            <a href="@Url.Action("TimeScheduleList", "Event")" class="btn btn-default">
                                <i class="icon-arrow-left13 position-left"></i>
                                <span>
                                    <strong>Back to List</strong>
                                </span>
                            </a>
                        </span>
                        <button type="submit" id="btncreate" name="Command" class="btn bg-slate-700"><strong>Create</strong> <i class="icon-arrow-right14 position-right"></i></button>
                    </div>
                </div>
            </div>
        }
        <!-- /basic layout -->

    </div>
</div>



@section Scripts {

    <link href="~/Scripts/src/clockpicker.css" rel="stylesheet" />
    <script src="~/Scripts/src/clockpicker.js"></script>

    <script type="text/javascript">

        $('#timetext,#endtime').clockpicker({
            placement: 'bottom',
            align: 'left',
            donetext: 'Done',
            autoclose: 'true'
        });

    </script>
<script type="text/javascript">
    $(function () {

        $('#Durationtext').keypress(function (event) {

            if (event.which == 8 || event.keyCode == 37 || event.keyCode == 39 || event.keyCode == 46)
                return true;

            else if ((event.which != 46 || $(this).val().indexOf('.') != -1) && (event.which < 48 || event.which > 57))
                event.preventDefault();
            //var start = $('#timetext').val();
            //var duration = $('#Durationtext').val();
            //var start = "12:00 AM";
            //date = new Date();
            //start = start.split(" ");
            //start[0] = start[0].split(":");
            //date.setHours(start[0][0]);
            //date.setMinutes(start[0][1]);
            //if (start[1] == "PM")
            //    date.setHours(date.getHours() + 12);

            //inputTimeInMin = 70;
            //date.setMinutes(date.getMinutes() + inputTimeInMin);

            //endtime = date.getHours() + ":" + date.getMinutes();
            //$('#endtime').val(endtime);
        });
    })
</script>

    
}